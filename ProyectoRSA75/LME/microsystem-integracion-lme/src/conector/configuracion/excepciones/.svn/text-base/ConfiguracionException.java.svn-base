/**
 * 
 */
package conector.configuracion.excepciones;

import conector.excepciones.SysException;

/**
 * Excepción genérica que indica algún problema con la configuración. El
 * administrador del sistema debe corregir la configuración para que la
 * aplicación pueda funcionar.
 * 
 * @author amartoq@microsystem.cl
 */
public class ConfiguracionException extends SysException {
    /** Serial Version UID. */
    private static final long serialVersionUID = 1L;

    /**
     * Crea una nueva excepción de configuración.
     * 
     * @param throwable
     *            excepción origen.
     * @param mensaje
     *            mensaje para el operador.
     */
    protected ConfiguracionException(Throwable throwable, String mensaje) {
        super(throwable, mensaje);
    }
}
